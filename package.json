{
  "name": "binance-proxy",
  "version": "1.0.0",
  "description": "Forwarding proxy for Binance public market data to bypass HTTP 451 geo-block for Google Apps Script requests",
  "main": "server.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
